meta {
  name: create entity
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}{{API_PATH}}/entities
  body: json
  auth: inherit
}

body:json {
  {
    "tag_uid": null,
    "name": "Sig Sauer P365",
    "parent_id": 6
  }
}

vars:post-response {
  ~tag_id: res.body.tag_id
  ~entity_id: res.body.id
}
