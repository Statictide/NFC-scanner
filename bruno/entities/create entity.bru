meta {
  name: create entity
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}{{API_PATH}}/entities
  body: json
  auth: inherit
}

body:json {
  {
    "parent_id": 1,
    "name": "Sig Sauer P365",
    "tag_uid": null
  }
}

vars:post-response {
  ~tag_id: res.body.tag_id
  ~entity_id: res.body.id
}
