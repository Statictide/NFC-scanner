meta {
  name: get entities
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/v1/entities
  body: none
  auth: none
}

vars:post-response {
  tag_id: res.body[0].tag_id
  entity_id: res.body[0].id
}
